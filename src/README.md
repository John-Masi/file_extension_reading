# file_extension_reading
C++ program containing methods of reading files(png), to be added(pcapng)

For PNG:
Reads the png file and puts it into a vector of bytes(uint8_t) then we read IHDR Data from the png
